<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="co.yedam.mapper.BoardMapper">
	<select id ="getTimes" resultType="String">
		select sysdate from dual
	</select>
	<select id="selectList" parameterType="int" resultType="co.yedam.vo.BoardVO">
		select a.*
		from (select rownum rn, b.*
    	from tbl_board b
    	<where>
    	<choose>
    		<when test="searchCondition == 'T'.toString()">
    			title like '%'||#{keyword}||'%'
    		</when>
    		<when test="searchCondition == 'W'.toString()">
    			writer like '%'||#{keyword}||'%'
    		</when>
    		<when test="searchCondition == 'TW'.toString()">
    		(writer like '%'||#{keyword}||'%' or title like '%'||#{keyword}||'%')
    		</when>
    		<otherwise>
    			1 = 1
    		</otherwise>
    	</choose>
    	</where>
    	order by board_no) a
	where rn > (#{page} -1)* 5
	<![CDATA[
	and rn <= (#{page} *5)
	]]>
	</select>
	<select id="slectCount" resultType="int">
		select count(1)
		from tbl_board
	</select>
	<insert id="insertBoard" parameterType="co.yedam.vo.BoardVO">
		insert into tbl_board(board_no, title, content, writer)
		values(board_seq.nextval, #{title}, #{content}, #{writer})
	</insert>
	<delete id="deleteBoard" parameterType="int">
		delete from tbl_board
		where board_no = #{bno}
	</delete>
	<update id="updateBoard" parameterType="co.yedam.vo.BoardVO">
	 UPDATE tbl_board
   	 SET title = #{title},
        content = #{content}
   	 WHERE board_number = #{board_no}
	</update>
	<select id="getBoard" parameterType="int" resultType="co.yedam.vo.BoardVO">
	 select *
   	 from tbl_board
   	 where board_no = #{bno}
	</select>
</mapper>